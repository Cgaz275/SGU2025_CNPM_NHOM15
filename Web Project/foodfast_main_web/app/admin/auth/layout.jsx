export const metadata = {
    title: "FoodFast - Admin Sign In",
    description: "Admin Portal Sign In",
};

export default function AdminAuthLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
